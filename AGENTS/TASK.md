# 工作须知
- 每次工作前都阅读/AGENTS路径下的AGENTS.md与PROJECT_STRUCTURE_SKILL.md文档内容。
- 严格并遵守上述两个文件的规定。
- UI风格严格统一。
- 每次代码只允许在当前模块进行修改（拆解的字模块），禁止对其他模块进行污染，如果有明确的指令可以忽略此条。


## 任务描述
- 在后台管理页面中的预警管理新增一个TAB叫重点区域。
- 这个TAB主要描述碰撞模型（碰撞预警）在指定区域内的运行情况，目前只考虑外高桥（也叫吴淞口）辖区下的圆圆沙警戒区与吴淞口警戒区。
- 辖区（外高桥）和特殊区域（圆圆沙、吴淞口）都支持切换。
- 样式风格与预警管理中其他TAB保持一致。
- 不需要类似指数这类不清不楚的统计方式。

## 需求描述
- 围绕碰撞预警的次数进行统计。
- 时间维度统计：自定义时间，最近24小时态势。
- 船舶类型维度统计：统计预警船舶的类型。
- 重点船舶维度统计：和上述需求基本一致，但只统计重点船舶（危险品、油船、客船、游轮）。
- 交通流量统计：这里不用统计预警数据，只统计流量数据即可，但要考虑时间维度。
- 尽量一屏展示，不要出现滚动条。






