import { useState } from 'react';\nimport { Card } from '@/components/ui/card';\nimport { Badge } from '@/components/ui/badge';\nimport { Button } from '@/components/ui/button';\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';\nimport {\n  AlertCircle,\n  AlertTriangle,\n  CheckCircle,\n  Clock,\n  MapPin,\n  Zap,\n  Users,\n  Anchor,\n  Wind,\n  Waves,\n  ChevronRight,\n} from 'lucide-react';\n\ninterface EmergencyCase {\n  id: string;\n  title: string;\n  vessel: string;\n  mmsi: string;\n  location: { lat: number; lon: number };\n  severity: 'critical' | 'high' | 'medium';\n  status: 'detected' | 'converging' | 'matching' | 'dispatching' | 'executing' | 'closed';\n  detectedTime: string;\n  description: string;\n  matchedPlan: string;\n}\n\nexport default function EmergencyResponse() {\n  const [selectedCase, setSelectedCase] = useState<string>('EMG-2024-001');\n\n  // 应急案例数据\n  const emergencyCases: EmergencyCase[] = [\n    {\n      id: 'EMG-2024-001',\n      title: '船舶碰撞险情',\n      vessel: '长江轮',\n      mmsi: '413123456',\n      location: { lat: 30.5, lon: 121.8 },\n      severity: 'critical',\n      status: 'executing',\n      detectedTime: '2024-05-18 10:45',\n      description: '长江轮与浦东号在长江口发生碰撞，船体受损，船员伤亡',\n      matchedPlan: '海上碰撞应急预案',\n    },\n    {\n      id: 'EMG-2024-002',\n      title: '油污泄漏',\n      vessel: '东海轮',\n      mmsi: '413345678',\n      location: { lat: 30.48, lon: 121.75 },\n      severity: 'high',\n      status: 'dispatching',\n      detectedTime: '2024-05-18 11:20',\n      description: '东海轮燃油舱破损，约 50 吨燃油泄漏',\n      matchedPlan: '油污应急预案',\n    },\n    {\n      id: 'EMG-2024-003',\n      title: '船舶失火',\n      vessel: '宝山轮',\n      mmsi: '413456789',\n      location: { lat: 30.55, lon: 121.85 },\n      severity: 'critical',\n      status: 'matching',\n      detectedTime: '2024-05-18 09:15',\n      description: '宝山轮货舱起火，火势蔓延',\n      matchedPlan: '船舶火灾应急预案',\n    },\n  ];\n\n  const currentCase = emergencyCases.find((c) => c.id === selectedCase) || emergencyCases[0];\n\n  const getSeverityColor = (severity: string) => {\n    switch (severity) {\n      case 'critical':\n        return 'bg-red-500/20 text-red-400 border-red-500/30';\n      case 'high':\n        return 'bg-orange-500/20 text-orange-400 border-orange-500/30';\n      case 'medium':\n        return 'bg-amber-500/20 text-amber-400 border-amber-500/30';\n      default:\n        return 'bg-secondary/50 text-foreground';\n    }\n  };\n\n  const getSeverityLabel = (severity: string) => {\n    switch (severity) {\n      case 'critical':\n        return '特别重大';\n      case 'high':\n        return '重大';\n      case 'medium':\n        return '中等';\n      default:\n        return '未知';\n    }\n  };\n\n  const getStatusColor = (status: string) => {\n    switch (status) {\n      case 'detected':\n        return 'bg-blue-500/20 text-blue-400 border-blue-500/30';\n      case 'converging':\n        return 'bg-cyan-500/20 text-cyan-400 border-cyan-500/30';\n      case 'matching':\n        return 'bg-purple-500/20 text-purple-400 border-purple-500/30';\n      case 'dispatching':\n        return 'bg-amber-500/20 text-amber-400 border-amber-500/30';\n      case 'executing':\n        return 'bg-orange-500/20 text-orange-400 border-orange-500/30';\n      case 'closed':\n        return 'bg-green-500/20 text-green-400 border-green-500/30';\n      default:\n        return 'bg-secondary/50 text-foreground';\n    }\n  };\n\n  const getStatusLabel = (status: string) => {\n    switch (status) {\n      case 'detected':\n        return '已发现';\n      case 'converging':\n        return '信息汇聚';\n      case 'matching':\n        return '预案匹配';\n      case 'dispatching':\n        return '资源调度';\n      case 'executing':\n        return '执行中';\n      case 'closed':\n        return '已关闭';\n      default:\n        return '未知';\n    }\n  };\n\n  const processSteps = [\n    { label: '险情发现', status: 'completed' },\n    { label: '态势汇聚', status: currentCase.status !== 'detected' ? 'completed' : 'pending' },\n    { label: '预案匹配', status: ['matching', 'dispatching', 'executing', 'closed'].includes(currentCase.status) ? 'completed' : 'pending' },\n    { label: '资源调度', status: ['dispatching', 'executing', 'closed'].includes(currentCase.status) ? 'completed' : 'pending' },\n    { label: '任务跟踪', status: ['executing', 'closed'].includes(currentCase.status) ? 'completed' : 'pending' },\n    { label: '过程留痕', status: 'pending' },\n    { label: '复盘评估', status: currentCase.status === 'closed' ? 'completed' : 'pending' },\n  ];\n\n  return (\n    <div className=\"min-h-screen bg-background text-foreground\">\n      {/* 顶部导航 */}\n      <header className=\"border-b border-border bg-card/50 backdrop-blur-sm sticky top-0 z-50\">\n        <div className=\"max-w-7xl mx-auto px-4 py-4\">\n          <div className=\"flex items-center gap-3 mb-4\">\n            <AlertCircle className=\"w-6 h-6 text-red-400 animate-pulse\" />\n            <h1 className=\"text-2xl font-bold text-foreground\">应急响应支持</h1>\n          </div>\n          <p className=\"text-sm text-muted-foreground\">险情发现、态势汇聚、预案匹配、资源调度的全流程支持</p>\n        </div>\n      </header>\n\n      {/* 主内容区域 */}\n      <main className=\"max-w-7xl mx-auto px-4 py-8\">\n        <div className=\"grid grid-cols-1 lg:grid-cols-4 gap-6\">\n          {/* 左侧：案例列表 */}\n          <div className=\"lg:col-span-1\">\n            <Card className=\"p-4 sticky top-24\">\n              <h3 className=\"text-lg font-bold text-foreground mb-4\">应急案例</h3>\n              <div className=\"space-y-2\">\n                {emergencyCases.map((caseItem) => (\n                  <button\n                    key={caseItem.id}\n                    onClick={() => setSelectedCase(caseItem.id)}\n                    className={`w-full text-left p-3 rounded-lg border transition-all ${\n                      selectedCase === caseItem.id\n                        ? 'bg-red-500/20 border-red-500'\n                        : 'bg-secondary/50 border-border hover:border-red-500/50'\n                    }`}\n                  >\n                    <div className=\"flex items-start justify-between mb-2\">\n                      <p className=\"font-medium text-foreground text-sm\">{caseItem.title}</p>\n                      <Badge\n                        className={`text-xs ${getSeverityColor(caseItem.severity)}`}\n                        variant=\"outline\"\n                      >\n                        {getSeverityLabel(caseItem.severity)}\n                      </Badge>\n                    </div>\n                    <p className=\"text-xs text-muted-foreground mb-1 font-mono\">{caseItem.id}</p>\n                    <p className=\"text-xs text-muted-foreground line-clamp-1\">{caseItem.vessel}</p>\n                  </button>\n                ))}\n              </div>\n            </Card>\n          </div>\n\n          {/* 中间和右侧：主内容 */}\n          <div className=\"lg:col-span-3 space-y-6\">\n            {/* 案例概览 */}\n            <Card className=\"p-6 border-l-4 border-l-red-500 bg-gradient-to-r from-red-500/5 to-transparent\">\n              <div className=\"flex items-start justify-between mb-4\">\n                <div>\n                  <h3 className=\"text-xl font-bold text-foreground mb-2\">{currentCase.title}</h3>\n                  <p className=\"text-sm text-muted-foreground\">案例编号：{currentCase.id}</p>\n                </div>\n                <Badge\n                  className={`${getSeverityColor(currentCase.severity)}`}\n                  variant=\"outline\"\n                >\n                  {getSeverityLabel(currentCase.severity)}\n                </Badge>\n              </div>\n              <div className=\"grid grid-cols-2 gap-4 mb-4\">\n                <div>\n                  <p className=\"text-xs text-muted-foreground mb-1\">涉及船舶</p>\n                  <p className=\"text-sm font-medium text-foreground\">{currentCase.vessel}</p>\n                </div>\n                <div>\n                  <p className=\"text-xs text-muted-foreground mb-1\">船舶 MMSI</p>\n                  <p className=\"text-sm font-mono text-foreground\">{currentCase.mmsi}</p>\n                </div>\n                <div>\n                  <p className=\"text-xs text-muted-foreground mb-1\">发现时间</p>\n                  <p className=\"text-sm text-foreground\">{currentCase.detectedTime}</p>\n                </div>\n                <div>\n                  <p className=\"text-xs text-muted-foreground mb-1\">当前状态</p>\n                  <Badge\n                    className={`${getStatusColor(currentCase.status)}`}\n                    variant=\"outline\"\n                  >\n                    {getStatusLabel(currentCase.status)}\n                  </Badge>\n                </div>\n              </div>\n              <div>\n                <p className=\"text-xs text-muted-foreground mb-2\">情况描述</p>\n                <p className=\"text-sm text-foreground\">{currentCase.description}</p>\n              </div>\n            </Card>\n\n            {/* 处理流程 */}\n            <Card className=\"p-6\">\n              <h3 className=\"text-lg font-bold text-foreground mb-4 flex items-center gap-2\">\n                <Zap className=\"w-5 h-5\" />\n                应急处置流程\n              </h3>\n              <div className=\"space-y-3\">\n                {processSteps.map((step, idx) => (\n                  <div key={idx} className=\"flex gap-4\">\n                    <div className=\"flex flex-col items-center\">\n                      <div\n                        className={`w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm ${\n                          step.status === 'completed'\n                            ? 'bg-green-500/20 text-green-400'\n                            : step.status === 'pending'\n                              ? 'bg-secondary/50 text-muted-foreground'\n                              : 'bg-amber-500/20 text-amber-400'\n                        }`}\n                      >\n                        {step.status === 'completed' ? '✓' : idx + 1}\n                      </div>\n                      {idx < processSteps.length - 1 && <div className=\"w-0.5 h-8 bg-border mt-2\" />}\n                    </div>\n                    <div className=\"flex-1 pb-4\">\n                      <p className=\"font-medium text-foreground\">{step.label}</p>\n                      <p className=\"text-xs text-muted-foreground mt-1\">\n                        {step.status === 'completed' ? '已完成' : '待处理'}\n                      </p>\n                    </div>\n                  </div>\n                ))}\n              </div>\n            </Card>\n\n            {/* 预案信息 */}\n            <Card className=\"p-6\">\n              <h3 className=\"text-lg font-bold text-foreground mb-4\">匹配预案</h3>\n              <div className=\"p-4 rounded-lg bg-secondary/50 border border-primary/30 mb-4\">\n                <p className=\"text-sm font-medium text-foreground mb-2\">{currentCase.matchedPlan}</p>\n                <p className=\"text-xs text-muted-foreground\">预案已自动匹配，包含应急资源、处置流程、联系方式等信息</p>\n              </div>\n              <div className=\"grid grid-cols-2 gap-4\">\n                <div className=\"p-3 rounded-lg bg-secondary/50 border border-border/50\">\n                  <p className=\"text-xs text-muted-foreground mb-2\">应急资源</p>\n                  <p className=\"text-sm font-medium text-foreground\">海事执法船 × 2</p>\n                </div>\n                <div className=\"p-3 rounded-lg bg-secondary/50 border border-border/50\">\n                  <p className=\"text-xs text-muted-foreground mb-2\">预计到达</p>\n                  <p className=\"text-sm font-medium text-foreground\">15 分钟</p>\n                </div>\n                <div className=\"p-3 rounded-lg bg-secondary/50 border border-border/50\">\n                  <p className=\"text-xs text-muted-foreground mb-2\">现场指挥</p>\n                  <p className=\"text-sm font-medium text-foreground\">海事部门主任</p>\n                </div>\n                <div className=\"p-3 rounded-lg bg-secondary/50 border border-border/50\">\n                  <p className=\"text-xs text-muted-foreground mb-2\">联系电话</p>\n                  <p className=\"text-sm font-medium text-foreground\">021-XXXX-XXXX</p>\n                </div>\n              </div>\n            </Card>\n\n            {/* 实时监控 */}\n            <Card className=\"p-6\">\n              <h3 className=\"text-lg font-bold text-foreground mb-4 flex items-center gap-2\">\n                <MapPin className=\"w-5 h-5\" />\n                事件位置与资源分布\n              </h3>\n              <div className=\"aspect-video rounded-lg bg-secondary/50 border border-border/50 flex items-center justify-center relative overflow-hidden\">\n                {/* 模拟地图 */}\n                <svg className=\"absolute inset-0 w-full h-full\" viewBox=\"0 0 400 300\" preserveAspectRatio=\"none\">\n                  <defs>\n                    <pattern id=\"mapGrid\" width=\"40\" height=\"40\" patternUnits=\"userSpaceOnUse\">\n                      <path d=\"M 40 0 L 0 0 0 40\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"0.5\" opacity=\"0.1\" />\n                    </pattern>\n                  </defs>\n                  <rect width=\"400\" height=\"300\" fill=\"url(#mapGrid)\" />\n                </svg>\n\n                {/* 事件标记 */}\n                <div className=\"absolute inset-0 flex items-center justify-center\">\n                  <div className=\"relative w-32 h-24\">\n                    {/* 事件位置 */}\n                    <div className=\"absolute left-1/2 top-1/2 transform -translate-x-1/2 -translate-y-1/2\">\n                      <div className=\"w-4 h-4 rounded-full bg-red-500 animate-pulse\" />\n                      <div className=\"absolute inset-0 w-4 h-4 rounded-full border-2 border-red-500 animate-ping\" />\n                    </div>\n\n                    {/* 资源位置 */}\n                    <div className=\"absolute left-1/3 top-1/3\">\n                      <Anchor className=\"w-5 h-5 text-green-400\" />\n                      <span className=\"text-xs text-green-400 mt-1 whitespace-nowrap\">执法船 1</span>\n                    </div>\n                    <div className=\"absolute right-1/4 bottom-1/3\">\n                      <Anchor className=\"w-5 h-5 text-green-400\" />\n                      <span className=\"text-xs text-green-400 mt-1 whitespace-nowrap\">执法船 2</span>\n                    </div>\n                  </div>\n                </div>\n              </div>\n              <p className=\"text-xs text-muted-foreground mt-3\">\n                事件位置：{currentCase.location.lat}°N, {currentCase.location.lon}°E\n              </p>\n            </Card>\n\n            {/* 操作按钮 */}\n            <div className=\"flex gap-3\">\n              <Button className=\"flex-1\" size=\"lg\" variant=\"destructive\">\n                <AlertCircle className=\"w-4 h-4 mr-2\" />\n                启动应急预案\n              </Button>\n              <Button variant=\"outline\" className=\"flex-1\" size=\"lg\">\n                <Users className=\"w-4 h-4 mr-2\" />\n                调度资源\n              </Button>\n            </div>\n          </div>\n        </div>\n      </main>\n    </div>\n  );\n}\n
